# Design handoffs

Design-agent exports and visual handoff assets live here so they stay separate
from app, Firebase, and web runtime code.

These folders are source/reference material. The production app only uses files
that are explicitly copied into `apps/`, `functions/`, `firebase/`, or `apps/web/`.
